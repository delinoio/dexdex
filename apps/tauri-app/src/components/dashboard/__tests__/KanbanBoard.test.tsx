// KanbanBoard was part of the old dashboard which has been replaced by WorkspaceHome.
// The new task list is shown in src/pages/WorkspaceHome.tsx
import { describe, it } from "vitest";

describe("KanbanBoard (legacy dashboard component)", () => {
  it("replaced by WorkspaceHome in src/pages/WorkspaceHome.tsx", () => {
    // The new workspace home groups tasks by status using UnitTaskCard components
  });
});
