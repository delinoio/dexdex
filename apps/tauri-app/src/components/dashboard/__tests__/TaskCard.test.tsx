// TaskCard was part of the old dashboard component which has been replaced.
// The new card component is src/components/task/UnitTaskCard.tsx
// Tests for it are in src/components/__tests__/UnitTaskCard.test.tsx
import { describe, it } from "vitest";

describe("TaskCard (legacy dashboard component)", () => {
  it("replaced by UnitTaskCard in src/components/task/UnitTaskCard.tsx", () => {
    // See src/components/__tests__/UnitTaskCard.test.tsx for tests
  });
});
