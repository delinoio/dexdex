// useTasks hook tests - updated for the new HTTP-based API
// Note: The hook has been moved to src/api/hooks/useTasks.ts and tests
// are now in src/api/__tests__/client.test.ts
import { describe, it } from "vitest";

describe("useTasks (legacy hook file)", () => {
  it("hook has been moved to src/api/hooks/useTasks.ts", () => {
    // Tests for the new API hooks are in src/api/__tests__/client.test.ts
    // See src/api/hooks/useTasks.ts for the new implementation
  });
});
